interface:
  display_name: "M Game Context"
  short_description: "Unity repo architecture, active gameplay stack, and guardrails."
  default_prompt: "Use this skill when working inside the M Game Unity project. Read the architecture references, start from the active ActionRPG character stack unless the task is clearly legacy-only, and preserve scene, save, inventory, singleton, status, animation, and VFX responsibilities before editing."
